let arr=["a","b","c"];
let arr1=["a","b","c"];
console.log(arr==arr1);
let arrc=arr;
console.log(arr==arrc);
console.log(arr[0])
console.log(arr[1])
//  Arrays store the address reference to the memory location where the array is stored 
//  if we compare two arrays with same elements then it will return us false value 
//  because the array variables store the address reference and the addresses in both arrays are different